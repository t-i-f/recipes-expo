path:
  - app
  - _layout.tsx
data:
  - find: |
      /^(.*<Stack>)$/m
    replace: |
      $1
        <Stack.Screen name="onboarding" options={{ headerShown: false }} />
    already: |
      name="onboarding"
